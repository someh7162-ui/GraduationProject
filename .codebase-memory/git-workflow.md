# 项目 Git 工作记忆

- 用户要求：涉及本项目的 Git 提交和远端操作，优先使用本地代理端口 `7897`。
- 代理地址：`http://127.0.0.1:7897`（Clash/Mihomo 当前实际监听端口；`7987` 是此前的误记）。
- 提交前检查：运行前端构建、相关测试，并执行 `git diff --check`。
- 当前聊天 UI 补全已包含 SSE、停止生成、重新生成、会话重命名/删除、代码高亮和资料检索模式；暗黑模式按用户要求不实现。
